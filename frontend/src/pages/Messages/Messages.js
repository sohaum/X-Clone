import React from "react";
import '../Page.css';

const Messages = () => {
    return (
        <div className="page"> 
            <h2 className="pageTitle">Welcome to Msg</h2>
        </div>
    );
};

export default Messages;